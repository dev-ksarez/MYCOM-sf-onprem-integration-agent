# Release 0.2.78

Hotfix release for the Admin UI connector save flow.

## Fixed

- Fixed `invalid assignment to const 'selectedConnectorNode'` when saving connector-related configuration while the dashboard graph filter fallback is evaluated.
