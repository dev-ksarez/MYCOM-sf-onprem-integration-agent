# Release 0.2.65

## Highlights

- Fixes empty dashboards after switching to a project/environment without a matching Salesforce instance.
- Shows an explicit context message in Overview, Scheduler, Connector and Run sections instead of silently rendering empty fallback lists.
- Aligns the header project/environment when a Salesforce instance is selected directly.

## Validation

- `npm run build`
