# Release 0.2.64

## Highlights

- Fixes the header project selector so project and environment selections are not overwritten by the currently selected or fallback Salesforce instance.
- Keeps the selected project visible even when no matching instance exists for the selected environment.

## Validation

- `npm run build`
