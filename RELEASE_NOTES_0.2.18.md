# Release 0.2.18

- Hotfix fuer Windows PowerShell: Die Rollen-Normalisierung in `install-agent-service.ps1` verwendet jetzt eine korrekt geklammerte Pipeline
- `update-existing-installation.cmd` kann die Dienstemigration damit auch auf Systemen mit Windows PowerShell ohne Parserfehler ausfuehren
- Die Rollenaufteilung aus `0.2.16` und der `nssm.exe`-Fix aus `0.2.17` bleiben unveraendert enthalten
