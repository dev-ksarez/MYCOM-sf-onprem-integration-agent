# Release 0.2.60

## Highlights

- Fixes PricebookEntry scheduler activation when `ProductCode` is used as the upsert field.
- Aligns UI and server-side required-field validation with the runtime Product2 lookup from ProductCode.

## Validation

- `npm run build`
